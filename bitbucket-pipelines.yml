pipelines:
  default:
    - step:
        name: Sync GitHub Mirror
        image: alpine/git:latest
        clone:
          enabled: false
        script:
          - git clone --bare git@github.com:romaswe/Fingerprinting.git
          - cd Fingerprinting.git
          - git push --mirror git@github.com:romaswe/Fingerprinting.git